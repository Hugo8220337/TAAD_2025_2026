"""
Package marker for `filters`.
This file keeps the `filters` directory a Python package so PyInstaller
and normal imports work correctly. It intentionally exposes the
abstract base class for convenience.
"""

